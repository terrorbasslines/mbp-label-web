---
import BaseLayout from "../layouts/BaseLayout.astro";
import ButtonLink from "../components/ButtonLink.astro";
import SectionHeader from "../components/SectionHeader.astro";
import ReleaseCard from "../components/ReleaseCard.astro";
import HomeLatestReleases from "../components/HomeLatestReleases.astro";
import HomeManagementTeam from "../components/HomeManagementTeam.astro";
import HomeRecentNews from "../components/HomeRecentNews.astro";
import { labelStats, releases, siteConfig } from "../data/site";

const homeJsonLd = {
  "@context": "https://schema.org",
  "@type": "MusicGroup",
  name: siteConfig.name,
  url: siteConfig.url,
  genre: ["Hardstyle", "Hard Dance", "Electronic Music", "Hardcore"],
  description: siteConfig.seoDescription,
  sameAs: siteConfig.socials.filter((social) => social.href.startsWith("http")).map((social) => social.href)
};

const mbpRegionCards = [
  { key: "europe", label: "MBP Europe", color: "#ffd000" },
  { key: "asia", label: "MBP Asia", color: "#ff1808" },
  { key: "america", label: "MBP America", color: "#23df1e" },
  { key: "world", label: "MBP World", color: "#bd00ff" },
  { key: "australia", label: "MBP Australia", color: "#1d27ff" }
];
---

<BaseLayout title={siteConfig.name} description={siteConfig.seoDescription} jsonLd={homeJsonLd}>
  <section class="stage-rail relative min-h-[92svh] overflow-hidden border-b border-white/10">
    <img src="/assets/brand/stage-hero.png" alt="" class="absolute inset-0 h-full w-full object-cover" fetchpriority="high" />
    <div class="absolute inset-0 bg-gradient-to-r from-black via-black/65 to-black/20"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-obsidian via-transparent to-black/30"></div>
    <div class="section-shell relative flex min-h-[92svh] items-center py-20">
      <div class="max-w-4xl">
        <p class="eyebrow">Electronic music label</p>
        <h1 class="display-title mt-5">{siteConfig.name}</h1>
        <p class="mt-6 max-w-2xl text-pretty text-lg leading-8 text-haze sm:text-xl">
          A hardstyle and electronic label platform built for powerful releases, serious artist development and a premium digital presence.
        </p>
        <div class="mt-8 flex flex-col gap-3 sm:flex-row">
          <ButtonLink href="/demo-submission/">Submit Demo</ButtonLink>
          <ButtonLink href="/releases/" variant="secondary">Explore Releases</ButtonLink>
        </div>
      </div>
    </div>
  </section>

  <section class="border-b border-white/10 bg-obsidian py-14">
    <div class="section-shell">
      <div class="grid gap-4 md:grid-cols-3">
        {
          labelStats.map((stat) => (
            <div class="rounded-lg border border-white/10 bg-white/[0.035] p-5">
              <p class="font-display text-4xl font-black uppercase leading-none text-white">{stat.value}</p>
              <p class="mt-2 text-sm uppercase tracking-[0.14em] text-steel">{stat.label}</p>
            </div>
          ))
        }
      </div>
      <div class="mt-4 grid gap-4 md:grid-cols-2">
        <div class="hidden rounded-lg border border-neon-cyan/25 bg-neon-cyan/[0.04] p-5" data-home-count-card>
          <p class="font-display text-4xl font-black uppercase leading-none text-white" data-home-artist-count>0</p>
          <p class="mt-2 text-sm uppercase tracking-[0.14em] text-steel">current artists</p>
        </div>
        <div class="hidden rounded-lg border border-neon-violet/30 bg-neon-violet/[0.05] p-5" data-home-count-card>
          <p class="font-display text-4xl font-black uppercase leading-none text-white" data-home-release-count>0</p>
          <p class="mt-2 text-sm uppercase tracking-[0.14em] text-steel">catalogue releases</p>
        </div>
      </div>
      <div class="mt-4 hidden grid gap-3 sm:grid-cols-2 lg:grid-cols-5" data-home-region-counts>
        {
          mbpRegionCards.map((region) => (
            <div class="rounded-lg border bg-black/35 p-4" style={`border-color:${region.color}55; box-shadow:0 0 18px ${region.color}14;`}>
              <p class="font-display text-3xl font-black uppercase leading-none text-white" data-home-region-count={region.key}>0</p>
              <p class="mt-2 text-xs font-black uppercase tracking-[0.14em]" style={`color:${region.color};`} data-home-region-label={region.key} data-home-region-name={region.label}>
                {region.label} releases
              </p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="py-20">
    <div class="section-shell">
      <div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
        <SectionHeader eyebrow="Latest releases" title="Newest MBP releases" copy="Listen through the newest official MBP catalogue releases with artwork, catalogue numbers and direct links to each available music platform." />
        <ButtonLink href="/releases/" variant="ghost">View All</ButtonLink>
      </div>
      <HomeLatestReleases />
      <div class="mt-10 hidden grid gap-5 sm:grid-cols-2 lg:grid-cols-4" data-home-static-releases>
        {releases.map((release) => <ReleaseCard release={release} />)}
      </div>
    </div>
  </section>

  <section class="border-y border-white/10 bg-black/40 py-20">
    <div class="section-shell">
      <HomeRecentNews />
    </div>
  </section>

  <section class="border-y border-white/10 bg-black/40 py-20">
    <div class="section-shell">
      <div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
        <SectionHeader eyebrow="Label management" title="Management and A&R team" copy="The core MBP leadership team behind label strategy, A&R, demo review, catalogue quality and artist relations." />
        <ButtonLink href="/artists/" variant="ghost">Meet Artists</ButtonLink>
      </div>
      <HomeManagementTeam />
    </div>
  </section>

  <section class="py-20">
    <div class="section-shell grid gap-10 lg:grid-cols-[0.92fr_1.08fr] lg:items-center">
      <div>
        <SectionHeader eyebrow="Demo submission" title="Send music with intent" copy="Send private streaming links or audio files directly to the MBP A&R inbox. Every serious demo is reviewed by the label team and answered by email." />
        <div class="mt-8 flex flex-col gap-3 sm:flex-row">
          <ButtonLink href="/demo-submission/">Open Demo Form</ButtonLink>
          <ButtonLink href="/contact/" variant="secondary">Contact Label</ButtonLink>
        </div>
      </div>
      <div class="clip-diagonal overflow-hidden border border-white/10 bg-black">
        <img src="/assets/brand/season4-banner.png" alt="The MasterBeat Project purple stage banner" class="h-full min-h-72 w-full object-cover" loading="lazy" />
      </div>
    </div>
  </section>

  <section class="border-t border-white/10 bg-rail-grid bg-[length:100%_100%,44px_44px] py-20">
    <div class="section-shell grid gap-8 lg:grid-cols-[0.7fr_1fr]">
      <SectionHeader eyebrow="About the label" title="Built for hard electronic releases" />
      <div class="max-w-3xl text-pretty text-lg leading-8 text-haze">
        <p>
          The MasterBeat Project is a hardstyle, hard dance and electronic music label built around clean catalogue presentation, serious A&R decisions and a strong visual world for artists who want their releases handled professionally.
        </p>
        <p class="mt-5">
          MBP connects release metadata, artwork, smart links, artist profiles and demo review into one public platform, so listeners, artists and search engines can understand the label catalogue without digging through disconnected posts.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
